# GRUB boot loader configuration

GRUB_DEFAULT={{ grub_default }}
GRUB_TIMEOUT={{ grub_timeout }}
GRUB_DISTRIBUTOR="{{ grub_distributor }}"
GRUB_CMDLINE_LINUX_DEFAULT="{{ grub_cmdline_linux_default }}"
GRUB_CMDLINE_LINUX="{{ grub_cmdline_linux }}"

# Preload both GPT and MBR modules so that they are not missed
GRUB_PRELOAD_MODULES="{{ grub_preload_modules }}"

# Uncomment to enable booting from LUKS encrypted devices
{% if grub_enable_cryptodisk is defined %}
GRUB_ENABLE_CRYPTODISK={{ grub_enable_cryptodisk }}
{% else %}
#GRUB_ENABLE_CRYPTODISK=y
{% endif %}

# Set to 'countdown' or 'hidden' to change timeout behavior,
# press ESC key to display menu.
GRUB_TIMEOUT_STYLE={{ grub_timeout_style }}

# Uncomment to use basic console
GRUB_TERMINAL_INPUT={{ grub_terminal_input }}

# Uncomment to disable graphical terminal
{% if grub_terminal_output is defined %}
GRUB_TERMINAL_OUTPUT={{ grub_terminal_output }}
{% else %}
#GRUB_TERMINAL_OUTPUT=console
{% endif %}

# The resolution used on graphical terminal
# note that you can use only modes which your graphic card supports via VBE
# you can see them in real GRUB with the command `vbeinfo'
GRUB_GFXMODE={{ grub_gfxmode }}

# Uncomment to allow the kernel use the same resolution used by grub
GRUB_GFXPAYLOAD_LINUX={{ grub_gfxpayload_linux }}

# Uncomment if you want GRUB to pass to the Linux kernel the old parameter
# format "root=/dev/xxx" instead of "root=/dev/disk/by-uuid/xxx"
{% if grub_disable_linux_uuid is defined %}
GRUB_DISABLE_LINUX_UUID={{ grub_disable_linux_uuid }}
{% else %}
#GRUB_DISABLE_LINUX_UUID=true
{% endif %}

# Uncomment to disable generation of recovery mode menu entries
GRUB_DISABLE_RECOVERY={{ grub_disable_recovery }}

# Uncomment and set to the desired menu colors.  Used by normal and wallpaper
# modes only.  Entries specified as foreground/background.
{% if grub_color_normal is defined %}
GRUB_COLOR_NORMAL="{{ grub_color_normal }}"
{% else %}
#GRUB_COLOR_NORMAL="light-blue/black"
{% endif %}
{% if grub_color_highlight is defined %}
GRUB_COLOR_HIGHLIGHT="{{ grub_color_highlight }}"
{% else %}
#GRUB_COLOR_HIGHLIGHT="light-cyan/blue"
{% endif %}

# Uncomment one of them for the gfx desired, a image background or a gfxtheme
{% if grub_background is defined %}
GRUB_BACKGROUND="{{ grub_background }}"
{% else %}
#GRUB_BACKGROUND="/path/to/wallpaper"
{% endif %}
{% if grub_theme is defined %}
GRUB_THEME="{{ grub_theme }}"
{% else %}
#GRUB_THEME="/path/to/gfxtheme"
{% endif %}

# Uncomment to get a beep at GRUB start
{% if grub_init_tune is defined %}
GRUB_INIT_TUNE="{{ grub_init_tune }}"
{% else %}
#GRUB_INIT_TUNE="480 440 1"
{% endif %}

# Uncomment to make GRUB remember the last selection. This requires to
# set 'GRUB_DEFAULT=saved' above.
{% if grub_savedefault is defined %}
GRUB_SAVEDEFAULT="{{ grub_savedefault }}"
{% else %}
#GRUB_SAVEDEFAULT="true"
{% endif %}
