---
# defaults file for mount
mount_mountpoint: /mnt

mount_list:
  - src: PARTLABEL=boot
    mountpoint: /boot
    fstype: vfat
    opts: defaults
  - src: PARTLABEL=root
    mountpoint: /
    fstype: ext4
    opts: defaults
